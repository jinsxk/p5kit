# p5kit CLI

Command line tools for running and building p5kit projects.

```sh
p5kit dev
p5kit build web
p5kit build ios
```

The current implementation focuses on the shared web bundle that native shells consume.
